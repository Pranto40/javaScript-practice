// জাভাস্ক্রিপ্টে দুই ধরনের ডাটা টাইপ রয়েছে। একটি হচ্ছে primitive  ডাটা টাইপ আরেকটি  হচ্ছে object।
// primitive ডাটা টাইপ এমন এক ডাটা টাইপ যেটা জাভাস্ক্রিপ্ট দিয়ে দেয় উদাহরণস্বরূপ (number, string, boolean, undefined, null)

// 1. number ( সকল ধরনের নাম্বার )
    const number = 38;
    const floatNumber = 40.10;
    // Number(), parseFloat(), parseInt(), NaN
    // Hexadecimal number (16) => const hexadecimal = 0X
    // Octal number (8) => const octal = 0

// 2. string ( সকল ধরনের text )
    const string1 = 'string';
    const string2 = "string";
    const string3 = `string`;
    // String() = conver to string, toString(),

// 3. boolean
    // Boolean()
    // true, false
// 4. undefind
// 5. null

// Falsy Values ("", 0, undefined null, NaN)


// object ডাটা টাইপ যেটা আমরা নিজেরা তৈরি করব জাভাস্ক্রিপ্ট keyword এর মাধ্যমে

// 1.Array
// 2.object
// 3.function